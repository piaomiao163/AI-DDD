<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.piaomiao.dal.mapper.PermissionMapper">
    <!-- 根据编码查询权限 -->
    <select id="selectByCode" parameterType="java.lang.String" resultType="com.piaomiao.dal.entity.PermissionDO">
        SELECT * FROM sys_permission WHERE code = #{code}
    </select>
    
    <!-- 查询所有权限 -->
    <select id="selectAll" resultType="com.piaomiao.dal.entity.PermissionDO">
        SELECT * FROM sys_permission
    </select>
    
    <!-- 根据角色ID查询权限 -->
    <select id="selectByRoleId" parameterType="java.lang.Long" resultType="com.piaomiao.dal.entity.PermissionDO">
        SELECT p.* FROM sys_permission p JOIN sys_role_permission rp ON p.id = rp.permission_id WHERE rp.role_id = #{roleId}
    </select>
    
    <!-- 根据菜单ID查询权限 -->
    <select id="selectByMenuId" resultType="com.piaomiao.dal.entity.PermissionDO">
        SELECT * FROM sys_permission WHERE menu_id = #{menuId}
    </select>
    
    <!-- 根据角色ID列表查询权限 -->
    <select id="selectByRoleIds" resultType="com.piaomiao.dal.entity.PermissionDO">
        SELECT p.*, rp.role_id as role_id
        FROM sys_permission p
        JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>
</mapper>